REDIS----------------------------------------------------------DESCRIPTIONRedis jest to rodzaj NoSql typu "Key Value". Oznacza to, że s³u¿y do przechowywania danych w postaci klucz-wartoæ. Dane te s¹ przechowywane w pamiêci RAM. Dzia³a na razie tylko pod Linux.Z Redis pracuje się za pomocą klientów - to oni łączą się z nim i wykonująwszystkie zadania. Tych klientów należy wykorzystać w swojej aplikacji,zarówno web jak i grubym kliencie. Dla Javy przykładowi klienci to:- Jedis;- JRedis;- JDBC-Redis;- itd.----------------------------------------------------------DOWNLOADObecne dostępny tylko pod Linux. Należy po prostu ściągnąć odpowidni plik i go rozpakować.Przykładowy plik to: redis-2.6.7.tar.gz----------------------------------------------------------INSTALLAby zainstalować Redis należy uruchomić terminal, udać się do lokacji <redis_home>i tam wywołać komendę: make----------------------------------------------------------RUNAby uruchomić Redis należy uruchomić terminal, udać się do likalizacji <redis_home>/srci tam wywołać komendę: ./redis-server----------------------------------------------------------OTHERStrony:http://redis.io/http://code.google.com/p/jredis/